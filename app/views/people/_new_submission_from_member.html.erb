<!-- erbstart app/views/people/_new_submission_from_member.html.erb -->
 <div class="modal-dialog">
   <div class="modal-content">
     <div class="modal-header">
       <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
       <h3 id="ModalLabel">Modal header</h3>
     </div>
     <%= form_for(:person, url: {action: 'update', id: @person.id},:html => {:class => 'form-horizontal'}) do |f| %>
       <div class="modal-body">
           <%= render(partial: 'form', locals: {f: f}) %>
       </div>
       <div class="modal-footer">
         <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
           <%= f.submit 'Save Changes', class: 'btn btn-primary' %>
       </div>
     <% end %>
   </div>
 </div>
<!-- erbend app/views/people/_new_submission_from_member.html.erb -->
