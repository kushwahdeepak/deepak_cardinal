
 <%= react_component("pages/MyConnection/MyConnectionIndex", {
    current_user: current_user.as_json(only: [:id, :job_title, :location, :name, :linkedin_profile_url, :github_url]),
    avatar_url: @avatar_url
  }) %>

<script src="https://api.cloudsponge.com/widget/<%= ENV.fetch("CLOUDSPONGE_KEY") %>.js"></script>

