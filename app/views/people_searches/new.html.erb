<!-- erbstart app/views/searches/new.html.erb -->
<%= react_component("pages/CandidateSearchPage/CandidateSearchPage", {
    currentUser: current_user,
    isEmailConfigured: @is_email_credential,
    user_id: current_user.id,
    jobs: @current_user_organization_jobs,
    currentOrganization: current_user.organization,
    memberOrganization: @member_organization
}) %>
<!-- erbend app/views/searches/new.html.erb -->
