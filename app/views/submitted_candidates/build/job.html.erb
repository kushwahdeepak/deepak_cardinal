<!-- erbstart app/views/submitted_candidates/build/job.html.erb -->
<%= form_for @submitted_candidate, method: :put, url: wizard_path do |f| %>
  <% if f.object.errors.any? %>
    <div class="error_messages">
      <% f.object.errors.full_messages.each do |error| %>
        <p><%= error %></p>
      <% end %>
    </div>
  <% end %>

  <fieldset>
    <legend>Pet Characteristics</legend>

    <div>
      <%= f.label :identifying_characteristics %>
      <br />
      <%= f.text_area :identifying_characteristics, rows: 10 %>
    </div>

    <div>
      <%= f.label :colour%>
      <%= f.text_field :colour %>
    </div>

    <div>
      <%= f.submit 'Next Step' %>
    </div>
  </fieldset>
<% end %>
<!-- erbend app/views/submitted_candidates/build/job.html.erb -->
