<!-- erbstart app/views/application/_employer_navbar.html.erb -->

<li>
  <%= link_to user_searched_candidates_path(current_user), style: 'text-shadow: 1px 1px 2px rgb(255, 255, 255, 0.1); align-self: flex-end;', alt: 'Just for you' do %>
    <div class="nav-link" style="float:left;"><span>My Candidates</span></div>
  <% end %>
</li>

  <li>

    <%= link_to jobs_path, style: 'text-shadow: 1px 1px 2px rgb(255, 255, 255, 0.1); align-self: flex-end;', alt: 'Just for you' do %>
    <div class="nav-link" style="float:left;"><span>Jobs</span></div>
    <% end %>
  </li>

  <li>
    <%= link_to new_people_search_path, style: 'text-shadow: 1px 1px 2px rgb(255, 255, 255, 0.1); align-self: flex-end;', alt: 'Just for you' do %>
      <div class="nav-link" style="float:left;"><span>Find Talent</span></div>
      <% unless current_user.follow_count == 0 %>
      <% end %>
    <% end %>
  </li>

  <% if current_user.follow_count != 0 %>

  <li style='margin-bottom: -15px;'>
    <%= link_to user_dashboard_path(current_user), style: 'text-shadow: 1px 1px 2px rgb(255, 255, 255, 0.1); align-self: flex-end;', alt: 'Just for you' do %>
      <div class="nav-link" style="float:left;"><span>My Interviews</span></div>
      <% unless current_user.follow_count == 0 %>
        <span class="badge"><%= current_user.follow_count %></span>
      <% end %>
    <% end %>
  </li>
   <% else %>

   <li>
     <%= link_to user_dashboard_path(current_user), style: 'text-shadow: 1px 1px 2px rgb(255, 255, 255, 0.1); align-self: flex-end;', alt: 'Just for you' do %>
       <div class="nav-link" style="float:left;"><span>My Interviews</span></div>
       <% unless current_user.follow_count == 0 %>
         <span class="badge"><%= current_user.follow_count %></span>
       <% end %>
     <% end %>
   </li>

   <% end %>

<!-- <li>
  <%= link_to welcome_employer_intro_path, style: 'text-shadow: 1px 1px 2px rgb(255, 255, 255, 0.1); align-self: flex-end;', alt: 'Profile' do %>
    <i class="fa fa-fw fa-users"></i>&nbsp;Company
  <% end %>
</li> -->


<li>
  <%= link_to url_for(edit_user_registration_path), style: 'align-self: flex-end;', alt: 'Settings' do %>

    <div class="nav-link" style="float:right;"><span>Settings</span></div>
  <% end %>
</li>


<li>
  <%= link_to destroy_user_session_path, method: :delete do %>
    <div class="nav-link btn btn-primary" style="float: left; margin-left: 0.5em; padding: 4px; margin-right: 0.5em; color: #fff; background-color: #CC0000; border-color: #CC0000; align-self: center; align-content: center;"><span>Sign Out</span></div>
  <% end %>
</li>
<!-- erbend app/views/application/_employer_navbar.html.erb -->
